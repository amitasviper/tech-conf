<nav class="navbar navbar-toggleable-md navbar-light bg-faded">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <%= link_to 'TechConf', root_path, :class => 'navbar-brand' %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to root_path, :class => 'nav-link' do %>
            Home
            <span class='sr-only'>amit</span>
        <% end %>

      </li>
      <li class="nav-item">
        <%= link_to 'Help', help_path, :class => 'nav-link' %>
      </li>
    </ul>

    <%= form_tag(search_path, :method => "get", class: "form-inline my-2 my-lg-0", id:"search-form") do %>
        <%= text_field_tag :query, params[:query], class: "form-control mr-sm-2", type: "text", placeholder: "Keywords" %>
        <%= text_field_tag :search_start_date, params[:search_start_date], class: "form-control mr-sm-2", type: "hidden", placeholder: "YYYY-MM-DD" %>
        <%= text_field_tag :search_end_date, params[:search_end_date], class: "form-control mr-sm-2", type: "hidden", placeholder: "YYYY-MM-DD" %>
        <%= submit_tag "Search", class: "btn btn-outline-success my-2 my-sm-0",:name => nil %>
    <% end %>



    <% if logged_in? %>
        <%= link_to 'Add Conference', new_conf_path, :class => 'nav-link' %>
        <div class="nav-item dropdown">
          <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown">
            Account <b class="caret"></b>
          </a>
          <div class="dropdown-menu">
            <%= link_to 'Profile', current_user, :class => 'dropdown-item' %>
            <%= link_to 'Settings', '#', :class => 'dropdown-item' %>
            <span class="divider"></span>
            <%= link_to 'Log out', logout_path, method: 'delete', :class => 'dropdown-item' %>
          </div>
        </div>
    <% else %>
        <%= link_to 'Log in', login_path, :class => 'nav-link' %>
    <% end %>
  </div>
</nav><br>
